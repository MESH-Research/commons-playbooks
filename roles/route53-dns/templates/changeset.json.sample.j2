{
  "Comment": "DNS records for zone {{ route53_zone_id }}",
  "Changes": [
    {
      "Action": "${ACTION}",
      "ResourceRecordSet": {
        "Name": "{{ wordpress_hostname }}",
        "Type": "A",
        "TTL": 300,
        "ResourceRecords": [
          {
            "Value": "${IP}"
          }
        ]
      }
    },
    {
      "Action": "${ACTION}",
      "ResourceRecordSet": {
        "Name": "*.{{ wordpress_hostname }}",
        "Type": "A",
        "TTL": 300,
        "ResourceRecords": [
          {
            "Value": "${IP}"
          }
        ]
      }
    }
  ]
}
