{
  "Comment": "DNS records for zone {{ route53_zone_id }}",
  "Changes": [
    {
      "Action": "UPSERT",
      "ResourceRecordSet": {
        "Name": "{{ wordpress_hostname }}",
        "Type": "A",
        "TTL": 300,
        "ResourceRecords": [
          {
            "Value": "{{ public_ip_address.stdout }}"
          }
        ]
      }
    },
    {
      "Action": "UPSERT",
      "ResourceRecordSet": {
        "Name": "*.{{ wordpress_hostname }}",
        "Type": "A",
        "TTL": 300,
        "ResourceRecords": [
          {
            "Value": "{{ public_ip_address.stdout }}"
          }
        ]
      }
    }
  ]
}
